//const host = process.env.NEXT_PUBLIC_API;
//export const apiUrl = host + "/backend";
//export const staticsUrl = host + "/backend/fotos_casos/";
//export const staticsPdf = host + "/backend/pdf_temp/";
//export const staticsCerts = host + "/backend/pdf_personal/";

const host = "http://localhost:3001";
export const apiUrl = host;
export const staticsUrl = host + "/fotos_casos/";
export const staticsPdf = host + "/pdf_temp/";
export const staticsCerts = host + "/pdf_personal/";
